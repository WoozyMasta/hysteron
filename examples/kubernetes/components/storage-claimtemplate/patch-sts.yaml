- op: remove
  path: /spec/template/spec/volumes/0
- op: add
  path: /spec/volumeClaimTemplates
  value:
    - metadata:
        name: data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 20Gi
